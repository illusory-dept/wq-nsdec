a utility to decrypt nscript.dat into ns.txt, and convert the result from
shift-jis to utf-8 using "iconv".

usage:
  place nscript.dat in the same dir as wq-nsdec.wq, then run:
    wq wq-nsdec.wq
  alternatively, you can manually edit the paths in the script.
